# 栖页 0.5.8：日程交互与 NodeSeek 修复

日期：2026-09-03

## 完成内容

- NodeSeek 站点、当前页签与最近关闭页签统一迁移到专用持久身份 `persist:qiye-nodeseek`，不再复用可能残留异常防护状态的默认身份。
- NodeSeek 专用身份使用当前 Chromium 对应的标准 Chrome User-Agent 和中文首选语言；防护重置保留为用户确认动作，只影响该专用身份。
- 周/月日程条目点击后显示就近悬浮详情卡，顶部修改、删除、关闭按钮与标题分行，不再相互遮挡。
- 创建日程右侧为独立的可滚动 24 小时时间轴；首次打开显示当前时间红线，开始时间变化后自动滚到对应时段。
- 未保存日程以半透明虚线阴影实时预览；创建日程中的旧逗号标签输入块已移除，统一内容标签保留。

## 验证结果

- NodeSeek 真实 Electron 专用分区：情报列表标题为“情报版块”，普通站内链接可进入帖子“关于Aff推广的规定（试行）”，主框架无加载错误。
- 完整自动检查：323 个单元测试全部通过；此前同一最终功能集的 39 个 Electron 集成测试全部通过，最终改动后的日程集成回归再次通过。
- 可见界面检查：时间轴可滚动、当前红线在首屏、18:30 草稿自动进入视野、阴影样式生效、旧标签块不存在；详情卡宽 378px，按钮可见且卡片保持在窗口内。
- 打包目录中的正式应用以隔离数据启动，退出码为 0；schema 为 18，浏览身份为 `default,sap-support,nodeseek`。

## 便携版

- 文件：`release/栖页-0.5.8-便携版.exe`
- 大小：100,072,294 字节
- SHA-256：`9D6F8E37ADD3FC76D75596073CA79BBFC67E1996AF56C80ED7AC20A60B72CD9A`

升级前请先退出仍在运行的 0.5.7，再启动 0.5.8；否则 Windows 单实例机制只会把旧窗口带到前台。NodeSeek 新身份首次使用可能需要重新登录一次。站点今后仍可能调整 Cloudflare 规则，栖页不会绕过验证码或安全检查。
