# 变更记录

## 0.5.7 - 2026-09-02

- 修复已实现的小序在既有用户数据中完全不可发现的问题：顶部增加全局小序入口；浏览网页时右侧 Rail 始终保留，即使小序尚未启用也可打开说明面板。
- 增加“启用基础小序”安全入口；该操作不会自动开启天气、护眼、喝水或活动提醒，也不会主动发送网页正文给 DeepSeek。
- 0.5.7 首次包含此前六阶段完成的小序状态机、天气、健康关怀、显式 DeepSeek 助手和本机运行态实现；schemaVersion 继续使用 18。

## 开发中 - 小序伴随助手 Sprint

- 在 Shell Renderer 中新增单实例 44px Companion Rail 与共用右侧面板；不依赖 WebContentsView 上方的 CSS z-index，不向远程网页注入小序 DOM，也不新增 Companion BrowserWindow/Renderer。
- 新增六状态机、低频前台/空闲活动判断、护眼/喝水/活动提醒、Cooldown、Snooze、安静时间、全屏/锁屏/睡眠静默及 Windows 桌面通知操作。
- 新增主进程 Open-Meteo Provider、30 分钟默认缓存、有限重试、15 秒超时、本地天气规则与 6 小时事件去重；未配置时不显示假天气。
- 复用已有 DeepSeek/OpenAI-compatible Provider 与同一 `safeStorage` 密钥，只有用户显式问答、总结当前页或解释选区时调用；登录/OAuth/支付页禁止总结，敏感工作站点首次发送需确认。
- 小序设置进入现有设置搜索、UIActionRegistry 和快捷键中心；城市、天气缓存、提醒运行态和敏感站点批准不进入 Google Drive，非敏感可移植偏好继续使用记录级同步。
- schemaVersion 从 17 升至 18；正式 App Version 保持 0.5.6，appId 与现有 Partition 不变。本 Sprint 未执行 electron-builder、打包、发布或生成 EXE/MSI。

## 开发中 - Google Drive 同步治理 Sprint

- Google Identity 与 Drive Sync 状态分离；旧 Token 缺少 `drive.appdata` 时保留账号登录并提示同账号重新授权。
- Desktop OAuth 使用系统浏览器、loopback 与 PKCE，实际授权 Scope 必须包含 `openid email profile drive.appdata`；Token 与导入后的 Client Secret 使用 `safeStorage`。
- 新增只读健康检查和用户主动触发的临时写入/删除测试，区分 API 未启用、Scope 缺失、测试用户、Token 过期、权限、限流和网络错误。
- 同步白名单扩展到计划、公开设置、栖页搜索/浏览历史和用户脚本元数据；敏感 URL 再清洗，删除使用 30 天 Tombstone。
- 增加 Manifest、设备/Revision/Checksum 与冲突治理；本地和云端同时变化时不再静默覆盖，覆盖前创建本机快照。
- 开发脚本与打包脚本分离；普通 CI、`build` 和 `build:code` 不再调用 electron-builder。
- 页签组织升级为本地持久化分组：顶部页签与左侧当前会话共用同一分组状态，支持拖入/拖出、折叠、排序、同空间合并和一次撤销，操作不重建 WebContents。
- 新增明确的 SAP/Zoho 站点家族候选和重复页签整理；相似页面只提示，完全重复项也必须由用户确认，关闭前检查下载、上传、播放、登录、Zoho 草稿与 `beforeunload`。
- 最近关闭页签保留原工作空间、分组、浏览身份与持久 Partition；可从页签菜单或 `Ctrl/Cmd + Shift + T` 恢复，原组已删除时安全回落为未分组。
- schemaVersion 升至 12；正式 App Version 仍为 0.5.5，时间轴和页签组默认不进入 Google Drive 同步白名单。

## 0.5.5 - 2026-08-30

### 通用搜索与设置中心

- 新增统一 `NavigationResolver` / `SearchService` / `SearchEngineRegistry` / `GlobalSearchService`：完整 URL、无协议域名、localhost、私网 IP 和普通关键词使用同一解析逻辑。脚本与本地协议始终阻止，未知自定义协议复用系统确认。
- 新增顶部 `Ctrl/Cmd + K` 搜索面板，本地搜索当前会话、我的站点、Chrome 书签和计划任务；不论本地是否命中，都保留用选定引擎搜索互联网的操作。输入期间不请求第三方搜索建议。
- 空浏览状态以搜索/网址输入为主操作；空状态打开一个页签，正常网页中的全局搜索新建页签，并继承当前空间与 BrowserProfile。临时页不会收藏或创建新分区。
- 内置 Google、Bing、百度和 DuckDuckGo，支持持久化默认引擎与本次临时引擎。本地历史去重、限制 100 条，删除 Token、OAuth Code、Session、密码等敏感 URL 参数。
- 设置页改为二级分类导航、右侧独立滚动和分类内折叠卡片；窄窗口使用顶部分类选择器。只挂载当前分类，Zoho 详细表单默认不创建。
- 新增基于 Settings Registry 的设置搜索、最后分类恢复、旧路由定位和未保存表单离开确认。schema 升级到 v10，不清理现有 Cookie、分区、连接器配置或授权。

## 0.5.4 - 2026-08-30

### SAP HANA 请求兼容

- 在完整栖页运行链中捕获到真正的 400 请求：打包后的 Electron User-Agent 含有非标准中文产品令牌 `栖页/0.5.3`，而此前成功探针只删除了英文包名，因此没有复现 HANA WAF 拒绝。
- 所有内嵌网页现在统一使用从当前 Chromium 版本生成的标准 ASCII Chrome User-Agent，不再暴露 Electron 或应用产品名；外部协议、浏览分区和站点登录 Cookie 保持不变。
- 用隔离数据在完整栖页中进入 SAP Support / SAP for Me 链路，原请求返回 HANA 400；应用修复后同一 Note 实际到达 `accounts.sap.com/saml2/idp/sso`，标题为“SAP for Me: 登录”。
- 新增打包产品名和 Electron 令牌清理回归，并继续覆盖 SAP 专用分区、会话重置以及非 SAP 登录隔离。

## 0.5.3 - 2026-08-30

### SAP 多页签会话重置

- 加固 SAP 专用分区的多页签重置顺序；后续完整应用请求跟踪确认这不是 HANA 400 的根因，真正的 User-Agent 兼容问题在 0.5.4 修复。
- “修复 SAP 登录”现在先停止全部 `sap-support` 页签、拆出窗口和受控弹窗，再终止在途连接并清空专用分区；避免其它 SAP 页签在清理过程中重新写回失效状态。
- SAP 专用分区清理后会检查 Cookie 数量为零，只恢复当前搜索或 Note 页签；其它 SAP 页签保留记录并进入休眠，切换时用干净身份重新加载。
- 默认浏览身份以及 Google、Chrome 书签、NodeSeek、Zoho、奶昔等非 SAP 会话完全不参与本次重置。
- 新增清理调用顺序、整分区 Cookie/Storage、Profile 弹窗隔离、全部 SAP 运行时暂停和真实 Electron 身份隔离回归。

## 0.5.2 - 2026-08-30

### SAP Support 独立登录身份

- 现场比对确认失败授权 URL 与干净 Electron 身份生成的 URL 完全一致，问题不是 ICP 搜索参数或地址栏改写，而是原共享浏览身份中的 SAP 状态仍会触发认证服务拒绝。
- SAP Support、SAP for Me、SAP ID 与 HANA 认证链改用独立持久分区 `persist:qiye-sap-support`；登录一次后在栖页内持续复用，同时不触碰 NodeSeek、Zoho、奶昔等原有登录状态。
- schema 升级到 v9，旧 SAP 固定站点和已打开会话可重复迁移到系统 SAP 身份；原 `persist:qiye-sites` 分区及其中 Cookie 不会被删除。
- “修复 SAP 登录”现在强制回到 SAP 独立身份，只在用户确认后清理该身份的 SAP Cookie/站点存储并重试原搜索或 Note。

## 0.5.1 - 2026-08-30

### SAP Support 登录修复

- 修复 SAP Support 搜索进入 SAP for Me 时可能停在 “potentially malicious character” 页面的问题。
- SAP 会话修复现在覆盖实际会影响 SSO 的 `.sap.com`、`.support.sap.com` 与 `.hana.ondemand.com` 父域 Cookie；仍只在用户点击并确认后执行，不影响 Chrome 或非 SAP 网站。
- 记录并保留原始 `me.sap.com/servicessupport/search/` 或 `me.sap.com/notes/` 目标，修复后重新进入原搜索结果，不把认证回调地址错误当成返回页。
- 增加真实 Electron Cookie 回归，验证 SAP 父域 Cookie 被移除而无关站点 Cookie 保留；线上空白分区只读探针确认 Electron 44 可正常到达 `accounts.sap.com/saml2/idp/sso`。

## 0.5.0 - 2026-08-30

### Phase 5：资源识别与内存治理

- 页面动作增加当前页资源扫描，识别显式下载、常见文件直链、图片、视频和音频；所有下载继续使用原生保存确认，不接受渲染层伪造 URL。
- 高级网络检测只能由用户显式开启，最长 60 秒，只记录当前 WebContents 的 URL、MIME 和大小；停止、导航或关闭时注销监听器。
- 明确拒绝 HLS/DASH 播放列表和 Key，不采集请求头、Cookie、Token 或响应正文，不绕过 DRM、登录、订阅、付费墙和验证码。
- 新增活动、预热、休眠、恢复和崩溃状态，以及省内存/标准/高性能三档 WebContents 池；标准模式默认保留 1 个活动和 1 个预热页签。
- 休眠销毁 WebContents 并清理翻译、用户脚本与资源运行时，但保持最近 URL、Cookie、LocalStorage、IndexedDB 和 `persist:qiye-sites`；播放、下载、上传、登录弹窗、未发送 Zoho 草稿与“保持运行”页签受到保护。
- 真实 Electron 探针验证 6 个 WebContents 降为 1 个，单次私有内存指标约下降 28%，恢复前后 Cookie 与持久分区不变。

### Phase 4：用户脚本与内置页面动作

- 新增 UserScript metadata、匹配、排除、运行时机、权限审批、脚本专属存储、远程 Hash/差异审查和隔离执行；不支持 `unsafeWindow`、远程依赖或任意 IPC。
- 自动化中心增加网页脚本管理；安装、更新和远程变化必须逐次审查确认，脚本异常隔离并记录脱敏状态。
- 普通登录页默认阻止第三方脚本；用户只有通过原生高级风险确认，才能按脚本和当前域名授权。OAuth/SAML 回调、支付、密码管理和内部页面仍永久禁止。
- 恢复“临时恢复复制与选择”内置动作，默认停用且按网站授权；不读取输入框、不联网，不绕过登录、订阅或付费墙。

### Phase 3：本地计划与桌面提醒

- 新增本周、本月、全部和已完成任务视图；任务支持空间、状态、优先级、截止时间、标签和本地时区。
- 新增本地提醒、5/10/30 分钟延后、免打扰、睡眠恢复、防重复通知、托盘菜单和显式开机启动；默认不改变原关闭行为。
- 任务、提醒和调度状态只写本机，固定站点和浏览会话不会被迁移成任务。

### Phase 2：划词与整页翻译

- 新增统一 Translation Provider Registry 与 OpenAI-compatible Provider，支持安全配置 Base URL、模型和 API Key；远程地址强制 HTTPS，本机 Provider 可使用 HTTP。
- 划词翻译增加网页右键和选区旁“译”按钮，显示源语言、原文、译文、服务、复制、重试和外部翻译入口；编辑区可插入或替换，密码框始终禁用。
- 整页翻译支持双语对照、仅译文、恢复原文、源/目标语言、站点规则、动态 DOM 和同源 iframe；跳过代码、表单、隐藏文字、纯数字、URL 与文件名，不替换 `innerHTML`。
- Zoho、SAP、邮箱、登录页和内部系统首次翻译前明确提示文本会发往 Provider；自动翻译敏感网站必须先持久授权。
- 翻译 API Key 进入现有 Electron `safeStorage` 加密凭据文件，页面正文和译文不写普通状态、数据库或日志；schema 升级到 v6，只保存公开配置和站点规则。
- 新增 Provider、密钥边界、选择动作、整页模式、动态页面、恢复原文和真实 Electron DOM 回归。

### Phase 1：浏览器基础与登录弹窗

- 新增网页原生上下文菜单，按选中文字、编辑区、链接、媒体和普通页面生成固定白名单操作；密码框不提供翻译或搜索，选中文字不进入渲染进程或日志。
- 地址栏支持网址与普通文字搜索、“粘贴并转到”，明确拒绝 `javascript:`、`data:` 和本地文件协议；补齐 F5、Esc 和 Alt+Right 快捷键。
- 新增窗口打开策略、受控登录弹窗和外部协议确认；OAuth、SSO 与 POST 窗口继承原 `browserProfileId` 和 `persist:qiye-sites`，保留 opener、POST 与 Cookie。
- 受控弹窗支持手动转为普通页签；脱敏跳转链只记录 Origin/Path，真实 Electron 夹具覆盖多次重定向、关闭、postMessage 和弹窗下载。
- 设置页增加站点级弹窗规则；schema 升级到 v5，但不清理旧标签、站点、Google/Zoho 配置或网站登录状态。
- 新增统一下载保存确认、安全文件名、连接安全状态以及真实 Electron popup/POST/redirect/postMessage 回归。

## 0.4.1 - 2026-08-30

### 侧栏与 Chrome 书签

- 移除左侧 Chrome 书签一级入口、底部重复书签卡和固定站点展示区；站点、固定状态、排序、工作空间归属及书签导入记录均保留。
- Chrome 书签搜索、文件夹筛选、Profile 选择、重新读取和已导入列表复用到“设置与数据”；旧 `bookmarks`/`chromeBookmarks` 入口自动定位该模块。
- 新增读取状态、当前 Profile、书签数量、最后读取时间、错误说明和带确认的本地导入记录清理。
- 当前会话区域使用剩余高度并独立滚动，Google 和设置入口固定在侧栏底部。

### 页签操作统一

- 顶部页签使用 Electron 原生菜单提供“复制页签、复制页面链接、在外部浏览器打开、关闭页签”，避免菜单被 `WebContentsView` 网页层遮挡；左侧会话继续使用应用内菜单。
- 顶部和左侧会话菜单统一调用主进程 `duplicateBrowserTab`；右键未激活页签不会预先激活，副本获得新 ID、插入源页签右侧并自动激活。
- 会话记录显式保存 `browserProfileId`，网页视图和登录弹窗从会话选择持久分区；当前唯一真实身份仍为 `persist:qiye-sites`，升级不会清理 Cookie。
- 增加侧栏结构、旧路由、Profile 导入/清理、原生页签菜单、目标页签、复制顺序、浏览身份、复制链接和外部打开回归。

## 0.4.0 - 2026-08-30

### 当前会话与固定站点

- 复用现有多标签浏览模型并升级为 schema v4 当前会话；记录来源空间、规范化 URL、标题、图标、创建/最后活动时间、加载和错误状态。
- 左侧栏明确分为跨空间“当前会话”和“固定站点 · 当前空间”；可切换为仅显示当前空间会话，并持久化偏好。
- 普通点击继续复用相同站点或规范化 URL，显式“新建会话”保留多实例；关闭会话不删除固定站点。
- 恢复顶部星星页面动作入口，继续只执行本机注册的白名单动作，不接受远程任意 JavaScript。

### 工单分诊与连接器

- 工作空间首页升级为 Zoho Desk 工单分诊台，包含五项真实指标、优先工单、最近工单、即将超时、连接状态和手动刷新；未连接时不显示假数量。
- 新增通用 Connector Registry、连接/执行服务、5 分钟缓存、标准错误、脱敏日志、ExternalObjectLink 和 context-isolated IPC。
- 新增 Zoho Desk 只读 OAuth/PKCE 客户端和只读查询：当前 Agent、分配工单、单张工单、最近公开线程、分页、超时、取消、令牌刷新和限流提示。
- 手动刷新期间按钮可切换为“取消刷新”；重试尊重限流等待，执行日志以同一 ID 从“运行中”更新为最终状态，不重复堆两条记录。
- Zoho Desktop 客户端密钥一次性导入 Electron `safeStorage` 后从普通 JSON 原子清除；访问/刷新令牌不进入渲染进程、普通状态文件或日志。
- 工单指标与排序迁移到独立领域模块；客户等待与 SLA 分开计算，缺失官方 SLA 时不模拟截止时间。
- Zoho 工单页面显示可收起的右侧上下文助手；Customer Ops、Emma、Gitee、B1 和回复草稿均显示真实未配置/未关联状态，不执行写入。

### 契约、迁移与边界

- 新增 Emma 与 Customer Ops 只读连接器契约；未确认 Emma 与现有 IMA 服务相同，因此未复用相邻插件凭据。
- 保留现有 Zoho-to-Gitee 插件职责；栖页不创建 Gitee Issue，只读取本地外部对象关联。
- v3 到 v4 迁移可重复执行并在首次迁移前备份；不把固定站点误迁移成会话，不清理 `persist:qiye-sites` 登录分区，也不重置 Google 状态。
- Google 状态点按语义显示：未配置/未连接为灰色、已连接为绿色、同步中为黄色、已配置但连接或同步失败才为红色。

## 0.3.3 - 2026-08-30

- Google OAuth 和 Drive 请求改用 Electron/Chromium 网络栈，自动沿用 Windows 系统代理，修复浏览器能登录但登录凭据交换连接失败的问题。
- 强制遵守 HTML `hidden` 属性，清除顶部意外露出的无效“当前页面动作”星星入口。
- Google 同步错误提示不再显示 Electron IPC 的内部方法前缀。

## 0.3.2 - 2026-08-29

### Google 数据同步

- 左下角新增紧凑的 Google 账号入口，支持连接、立即同步、从云端恢复和退出；界面明确区分“栖页数据同步”和“Chrome 书签回写”。
- 使用 Google Desktop OAuth、系统默认浏览器、随机 loopback 端口、PKCE S256 和 state 校验；只申请 `drive.appdata`、邮箱和 OpenID 最小权限。
- OAuth 客户端配置保存在应用本机数据目录，不写入源码或安装包；访问与刷新令牌使用 Electron `safeStorage` 加密并原子写入。
- Google Drive 仅保存固定的应用专属隐藏文件 `qiye-sync-v1.json`；同步空间、站点、收藏书签和可迁移设置。
- 云端快照严格白名单并清理 URL 中的认证参数；不上传 Cookie、网站登录会话、当前标签、浏览身份分区、最近访问、执行日志或 OAuth 凭据。
- 同步时比较本机与云端更新时间：较新一方更新较旧一方；内容相同不重复写入，时间相同但内容冲突时停止并提示。云端恢复前会在本机生成完整备份。

### 验证

- 增加 OAuth 类型、PKCE/state、令牌加密、Drive appDataFolder 请求、安全快照、损坏远端、同步决策、云端恢复隔离和 Google 界面全流程回归。

## 0.3.1 - 2026-08-29

### 修复与交互

- 左侧站点不再显示含义不清的右箭头，改为明确的标签操作按钮；右键任意站点入口可选择“打开”“复制为新标签”或“在独立窗口打开”。
- 用户明确复制时允许同一站点存在多个标签；普通点击仍自动定位已有标签。副本使用新的网页实例并共享原登录 Cookie，复制后从当前网址重新加载。
- 浏览器工具栏增加“复制标签”，并将“拆出”改名为“独立窗口”；修复 Windows 拖拽结束坐标为 `0,0` 时无法拆出的问题。
- 空间切换由主进程原子卸载旧网页并挂载目标空间网页，避免异步隐藏指令误卸载刚恢复的页面。
- 删除占用高度的“已连接/正在加载”信息条，加载状态仅保留在地址栏进度线；隐藏顶部“当前页面动作”按钮和抽屉。
- 标题栏常显版本号，便于识别正在运行的便携版版本。

### 验证

- 增加显式副本持久化、右键复制、工具栏复制、普通打开继续去重及 Windows 零坐标拖出回归。
- 真实 Electron 验证副本拥有不同 `webContentsId`、共享同一持久会话，并可跨重启保留。

## 0.3.0 - 2026-08-29

### 新增

- 每个空间拥有独立的网页标签组；同一站点再次打开时定位已有标签，同一临时网址重复打开时也会自动合并，不在空间内堆叠重复页面。
- 普通网页通过安全 GET 打开的新页面会进入当前空间标签组；`mailto:`、`tel:` 继续交给系统，带 POST 的登录弹窗保留原始请求语义。
- 标签支持按钮拆出和拖出到独立原生窗口，也可从主窗口或独立窗口合回原空间。
- 独立窗口提供后退、前进、刷新、地址栏和“合回空间”操作；点击主窗口中的独立标签可以重新聚焦窗口。

### 兼容与行为

- 拆出和合回移动的是同一个 `WebContentsView`，不重新加载网址；本次运行期间保留页面 DOM、滚动、表单内容和登录现场。
- 旧版每空间单页面快照会自动归一化为该空间的第一个标签；继续使用 schema v3 和原 `persist:qiye-sites` 会话，不清空站点、书签或 Cookie。
- 标签及当前网址会在本机恢复；独立窗口位置不跨重启，重启后标签统一回到原空间的主窗口。
- 直接关闭独立窗口等同于关闭该标签；使用“合回空间”则保留标签和页面现场。

### 验证

- 增加标签生命周期、站点与临时网址去重、跨空间隔离、弹窗归组、旧快照迁移、站点移动/删除、独立窗占位和同一网页进程拆出/合回回归。
- 保留 1480×920、1060×700、侧栏收纳、快速空间切换及原有助手/自动化/Chrome 导入回归。

## 0.2.1 - 2026-08-29

### 改进

- 每个空间使用独立的网页现场：本次运行期间切换后恢复该空间已经打开的页面、历史记录和 DOM 状态；从未打开网页的空间显示明确空态。应用重启后恢复站点和当前网址，不伪造历史栈。
- 三个空间仍共用原来的 `persist:qiye-sites` 登录会话，因此空间隔离网页内容时不会要求重新登录网站。
- 快速连续切换采用最后一次操作优先，旧响应和旧网页事件不会覆盖当前空间。
- 左侧导航支持收纳为 72px 图标栏，并记住展开或收纳状态；收纳后仍可切换空间和访问主要入口。
- 网站信息条由 54px 压缩为 38px，不再重复显示完整网页标题；本地会话提示移入地址栏。
- 空间切换提示只保留最新一条，成功提示约 1.4 秒后消失。

### 验证

- 增加多空间网页隔离、严格空态、快速切换、本地路由优先、两种窗口尺寸、侧栏收纳和原生网页边界回归。
- 每个 Electron 界面夹具使用独立临时用户目录，不接触真实栖页数据。

## 0.2.0 - 2026-08-29

### 新增

- 个人、工作、研究三个持久化空间与独立首页展示。
- 工作空间紧凑首页、真实最近访问、快捷动作和空状态。
- 站点的空间、类型、打开方式、固定状态和助手绑定字段。
- 自动化中心四个页签及真实助手执行记录。
- 声明式站点助手 Registry、Matcher、Permission Guard、Execution Service 和 Page Context Adapter。
- 通用页面动作和 Zoho Desk 工单助手 MVP。
- BrowserProfile 默认适配接口，以及 Manifest V3 浏览器扩展架构文档。
- v3 状态迁移、v2 备份、原子写入、损坏条目隔离和迁移错误记录。
- Node 内置单元测试与临时用户目录 Electron 集成回归。

### 兼容

- 旧站点、固定项和 Chrome 书签迁移到兼容模型，不改变原始 ID 和 URL。
- 继续使用 `persist:qiye-sites`，不主动清空或迁移现有登录会话。
- 保留奶昔签到调度、Chrome 导入、内置浏览器工具栏和旧自动化路由。
- 保留 NodeSeek 与 SAP 的显式站点修复能力。

### 安全

- 助手动作由主进程白名单执行，拒绝渲染进程注入页面上下文或脚本。
- 未配置连接器不执行、不伪报成功。
- 执行日志限制为 200 条并移除敏感字段、完整 URL、认证信息和正文。
