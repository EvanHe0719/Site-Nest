# 变更记录

## 0.5.0 - 开发中

### Phase 2：划词与整页翻译

- 新增统一 Translation Provider Registry 与 OpenAI-compatible Provider，支持安全配置 Base URL、模型和 API Key；远程地址强制 HTTPS，本机 Provider 可使用 HTTP。
- 划词翻译增加网页右键和选区旁“译”按钮，显示源语言、原文、译文、服务、复制、重试和外部翻译入口；编辑区可插入或替换，密码框始终禁用。
- 整页翻译支持双语对照、仅译文、恢复原文、源/目标语言、站点规则、动态 DOM 和同源 iframe；跳过代码、表单、隐藏文字、纯数字、URL 与文件名，不替换 `innerHTML`。
- Zoho、SAP、邮箱、登录页和内部系统首次翻译前明确提示文本会发往 Provider；自动翻译敏感网站必须先持久授权。
- 翻译 API Key 进入现有 Electron `safeStorage` 加密凭据文件，页面正文和译文不写普通状态、数据库或日志；schema 升级到 v6，只保存公开配置和站点规则。
- 新增 Provider、密钥边界、选择动作、整页模式、动态页面、恢复原文和真实 Electron DOM 回归。

### Phase 1：浏览器基础与登录弹窗

- 新增网页原生上下文菜单，按选中文字、编辑区、链接、媒体和普通页面生成固定白名单操作；密码框不提供翻译或搜索，选中文字不进入渲染进程或日志。
- 地址栏支持网址与普通文字搜索、“粘贴并转到”，明确拒绝 `javascript:`、`data:` 和本地文件协议；补齐 F5、Esc 和 Alt+Right 快捷键。
- 新增窗口打开策略、受控登录弹窗和外部协议确认；OAuth、SSO 与 POST 窗口继承原 `browserProfileId` 和 `persist:qiye-sites`，保留 opener、POST 与 Cookie。
- 设置页增加站点级弹窗规则；schema 升级到 v5，但不清理旧标签、站点、Google/Zoho 配置或网站登录状态。
- 新增统一下载保存确认、安全文件名、连接安全状态以及真实 Electron popup/POST/redirect/postMessage 回归。

## 0.4.1 - 2026-08-30

### 侧栏与 Chrome 书签

- 移除左侧 Chrome 书签一级入口、底部重复书签卡和固定站点展示区；站点、固定状态、排序、工作空间归属及书签导入记录均保留。
- Chrome 书签搜索、文件夹筛选、Profile 选择、重新读取和已导入列表复用到“设置与数据”；旧 `bookmarks`/`chromeBookmarks` 入口自动定位该模块。
- 新增读取状态、当前 Profile、书签数量、最后读取时间、错误说明和带确认的本地导入记录清理。
- 当前会话区域使用剩余高度并独立滚动，Google 和设置入口固定在侧栏底部。

### 页签操作统一

- 顶部页签使用 Electron 原生菜单提供“复制页签、复制页面链接、在外部浏览器打开、关闭页签”，避免菜单被 `WebContentsView` 网页层遮挡；左侧会话继续使用应用内菜单。
- 顶部和左侧会话菜单统一调用主进程 `duplicateBrowserTab`；右键未激活页签不会预先激活，副本获得新 ID、插入源页签右侧并自动激活。
- 会话记录显式保存 `browserProfileId`，网页视图和登录弹窗从会话选择持久分区；当前唯一真实身份仍为 `persist:qiye-sites`，升级不会清理 Cookie。
- 增加侧栏结构、旧路由、Profile 导入/清理、原生页签菜单、目标页签、复制顺序、浏览身份、复制链接和外部打开回归。

## 0.4.0 - 2026-08-30

### 当前会话与固定站点

- 复用现有多标签浏览模型并升级为 schema v4 当前会话；记录来源空间、规范化 URL、标题、图标、创建/最后活动时间、加载和错误状态。
- 左侧栏明确分为跨空间“当前会话”和“固定站点 · 当前空间”；可切换为仅显示当前空间会话，并持久化偏好。
- 普通点击继续复用相同站点或规范化 URL，显式“新建会话”保留多实例；关闭会话不删除固定站点。
- 恢复顶部星星页面动作入口，继续只执行本机注册的白名单动作，不接受远程任意 JavaScript。

### 工单分诊与连接器

- 工作空间首页升级为 Zoho Desk 工单分诊台，包含五项真实指标、优先工单、最近工单、即将超时、连接状态和手动刷新；未连接时不显示假数量。
- 新增通用 Connector Registry、连接/执行服务、5 分钟缓存、标准错误、脱敏日志、ExternalObjectLink 和 context-isolated IPC。
- 新增 Zoho Desk 只读 OAuth/PKCE 客户端和只读查询：当前 Agent、分配工单、单张工单、最近公开线程、分页、超时、取消、令牌刷新和限流提示。
- 手动刷新期间按钮可切换为“取消刷新”；重试尊重限流等待，执行日志以同一 ID 从“运行中”更新为最终状态，不重复堆两条记录。
- Zoho Desktop 客户端密钥一次性导入 Electron `safeStorage` 后从普通 JSON 原子清除；访问/刷新令牌不进入渲染进程、普通状态文件或日志。
- 工单指标与排序迁移到独立领域模块；客户等待与 SLA 分开计算，缺失官方 SLA 时不模拟截止时间。
- Zoho 工单页面显示可收起的右侧上下文助手；Customer Ops、Emma、Gitee、B1 和回复草稿均显示真实未配置/未关联状态，不执行写入。

### 契约、迁移与边界

- 新增 Emma 与 Customer Ops 只读连接器契约；未确认 Emma 与现有 IMA 服务相同，因此未复用相邻插件凭据。
- 保留现有 Zoho-to-Gitee 插件职责；栖页不创建 Gitee Issue，只读取本地外部对象关联。
- v3 到 v4 迁移可重复执行并在首次迁移前备份；不把固定站点误迁移成会话，不清理 `persist:qiye-sites` 登录分区，也不重置 Google 状态。
- Google 状态点按语义显示：未配置/未连接为灰色、已连接为绿色、同步中为黄色、已配置但连接或同步失败才为红色。

## 0.3.3 - 2026-08-30

- Google OAuth 和 Drive 请求改用 Electron/Chromium 网络栈，自动沿用 Windows 系统代理，修复浏览器能登录但登录凭据交换连接失败的问题。
- 强制遵守 HTML `hidden` 属性，清除顶部意外露出的无效“当前页面动作”星星入口。
- Google 同步错误提示不再显示 Electron IPC 的内部方法前缀。

## 0.3.2 - 2026-08-29

### Google 数据同步

- 左下角新增紧凑的 Google 账号入口，支持连接、立即同步、从云端恢复和退出；界面明确区分“栖页数据同步”和“Chrome 书签回写”。
- 使用 Google Desktop OAuth、系统默认浏览器、随机 loopback 端口、PKCE S256 和 state 校验；只申请 `drive.appdata`、邮箱和 OpenID 最小权限。
- OAuth 客户端配置保存在应用本机数据目录，不写入源码或安装包；访问与刷新令牌使用 Electron `safeStorage` 加密并原子写入。
- Google Drive 仅保存固定的应用专属隐藏文件 `qiye-sync-v1.json`；同步空间、站点、收藏书签和可迁移设置。
- 云端快照严格白名单并清理 URL 中的认证参数；不上传 Cookie、网站登录会话、当前标签、浏览身份分区、最近访问、执行日志或 OAuth 凭据。
- 同步时比较本机与云端更新时间：较新一方更新较旧一方；内容相同不重复写入，时间相同但内容冲突时停止并提示。云端恢复前会在本机生成完整备份。

### 验证

- 增加 OAuth 类型、PKCE/state、令牌加密、Drive appDataFolder 请求、安全快照、损坏远端、同步决策、云端恢复隔离和 Google 界面全流程回归。

## 0.3.1 - 2026-08-29

### 修复与交互

- 左侧站点不再显示含义不清的右箭头，改为明确的标签操作按钮；右键任意站点入口可选择“打开”“复制为新标签”或“在独立窗口打开”。
- 用户明确复制时允许同一站点存在多个标签；普通点击仍自动定位已有标签。副本使用新的网页实例并共享原登录 Cookie，复制后从当前网址重新加载。
- 浏览器工具栏增加“复制标签”，并将“拆出”改名为“独立窗口”；修复 Windows 拖拽结束坐标为 `0,0` 时无法拆出的问题。
- 空间切换由主进程原子卸载旧网页并挂载目标空间网页，避免异步隐藏指令误卸载刚恢复的页面。
- 删除占用高度的“已连接/正在加载”信息条，加载状态仅保留在地址栏进度线；隐藏顶部“当前页面动作”按钮和抽屉。
- 标题栏常显版本号，便于识别正在运行的便携版版本。

### 验证

- 增加显式副本持久化、右键复制、工具栏复制、普通打开继续去重及 Windows 零坐标拖出回归。
- 真实 Electron 验证副本拥有不同 `webContentsId`、共享同一持久会话，并可跨重启保留。

## 0.3.0 - 2026-08-29

### 新增

- 每个空间拥有独立的网页标签组；同一站点再次打开时定位已有标签，同一临时网址重复打开时也会自动合并，不在空间内堆叠重复页面。
- 普通网页通过安全 GET 打开的新页面会进入当前空间标签组；`mailto:`、`tel:` 继续交给系统，带 POST 的登录弹窗保留原始请求语义。
- 标签支持按钮拆出和拖出到独立原生窗口，也可从主窗口或独立窗口合回原空间。
- 独立窗口提供后退、前进、刷新、地址栏和“合回空间”操作；点击主窗口中的独立标签可以重新聚焦窗口。

### 兼容与行为

- 拆出和合回移动的是同一个 `WebContentsView`，不重新加载网址；本次运行期间保留页面 DOM、滚动、表单内容和登录现场。
- 旧版每空间单页面快照会自动归一化为该空间的第一个标签；继续使用 schema v3 和原 `persist:qiye-sites` 会话，不清空站点、书签或 Cookie。
- 标签及当前网址会在本机恢复；独立窗口位置不跨重启，重启后标签统一回到原空间的主窗口。
- 直接关闭独立窗口等同于关闭该标签；使用“合回空间”则保留标签和页面现场。

### 验证

- 增加标签生命周期、站点与临时网址去重、跨空间隔离、弹窗归组、旧快照迁移、站点移动/删除、独立窗占位和同一网页进程拆出/合回回归。
- 保留 1480×920、1060×700、侧栏收纳、快速空间切换及原有助手/自动化/Chrome 导入回归。

## 0.2.1 - 2026-08-29

### 改进

- 每个空间使用独立的网页现场：本次运行期间切换后恢复该空间已经打开的页面、历史记录和 DOM 状态；从未打开网页的空间显示明确空态。应用重启后恢复站点和当前网址，不伪造历史栈。
- 三个空间仍共用原来的 `persist:qiye-sites` 登录会话，因此空间隔离网页内容时不会要求重新登录网站。
- 快速连续切换采用最后一次操作优先，旧响应和旧网页事件不会覆盖当前空间。
- 左侧导航支持收纳为 72px 图标栏，并记住展开或收纳状态；收纳后仍可切换空间和访问主要入口。
- 网站信息条由 54px 压缩为 38px，不再重复显示完整网页标题；本地会话提示移入地址栏。
- 空间切换提示只保留最新一条，成功提示约 1.4 秒后消失。

### 验证

- 增加多空间网页隔离、严格空态、快速切换、本地路由优先、两种窗口尺寸、侧栏收纳和原生网页边界回归。
- 每个 Electron 界面夹具使用独立临时用户目录，不接触真实栖页数据。

## 0.2.0 - 2026-08-29

### 新增

- 个人、工作、研究三个持久化空间与独立首页展示。
- 工作空间紧凑首页、真实最近访问、快捷动作和空状态。
- 站点的空间、类型、打开方式、固定状态和助手绑定字段。
- 自动化中心四个页签及真实助手执行记录。
- 声明式站点助手 Registry、Matcher、Permission Guard、Execution Service 和 Page Context Adapter。
- 通用页面动作和 Zoho Desk 工单助手 MVP。
- BrowserProfile 默认适配接口，以及 Manifest V3 浏览器扩展架构文档。
- v3 状态迁移、v2 备份、原子写入、损坏条目隔离和迁移错误记录。
- Node 内置单元测试与临时用户目录 Electron 集成回归。

### 兼容

- 旧站点、固定项和 Chrome 书签迁移到兼容模型，不改变原始 ID 和 URL。
- 继续使用 `persist:qiye-sites`，不主动清空或迁移现有登录会话。
- 保留奶昔签到调度、Chrome 导入、内置浏览器工具栏和旧自动化路由。
- 保留 NodeSeek 与 SAP 的显式站点修复能力。

### 安全

- 助手动作由主进程白名单执行，拒绝渲染进程注入页面上下文或脚本。
- 未配置连接器不执行、不伪报成功。
- 执行日志限制为 200 条并移除敏感字段、完整 URL、认证信息和正文。
